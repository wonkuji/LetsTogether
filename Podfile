# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

workspace 'LetsTogether'

target 'LetsTogether' do
  project 'LetsTogether'

  use_frameworks!
  use_modular_headers!

  # Pods for SDKSample_Dynamic
  pod 'Firebase/Crashlytics', :configurations => ['Release']
  
  # (Recommended) Pod for Google Analytics
  pod 'Firebase/Analytics', :configurations => ['Release']

end
